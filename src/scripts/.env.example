# Environment Configuration Template for Firebase Scripts

This template is for the Firebase population scripts in the `src/scripts/` directory.

## Setup Instructions

1. Copy this file to `src/scripts/.env`
2. Fill in your Firebase credentials
3. Run the scripts using: `npm run <script-name>`

## Environment Variables

```bash
# Firebase Configuration
# Get these from your Firebase Console
FIREBASE_API_KEY=your_firebase_api_key_here
FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
FIREBASE_APP_ID=your_app_id
FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX
```

## Security Note

**NEVER commit the actual `.env` file to version control!**

The `.gitignore` file should include:
```
.env
src/scripts/.env
```
